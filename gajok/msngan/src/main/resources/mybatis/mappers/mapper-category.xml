<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.category">
   
	<select id="getAllCategory" resultType="com.coeding.springmvc.entity.Category">
	<![CDATA[
		SELECT * FROM categoryz ;
		
	]]>
	</select>
	<!-- <select id="findById" parameterType="int" resultType="com.coeding.springmvc.entity.Category">
	<![CDATA[
		SELECT * FROM productz where _id = #{id} ;
		
	]]>
	</select>
	 -->
	
</mapper>	
